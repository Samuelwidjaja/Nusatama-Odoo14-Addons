<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.11per12</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_untaxed']" position="after">
                <field name="eleven_twelve_untaxed_amount" widget="monetary" options="{'currency_field': 'currency_id'}" readonly="1"/>
            </xpath>

	   <xpath expr="//field[@name='eleven_twelve_untaxed_amount']" position="attributes">
            <attribute name="attrs">{'invisible': [('currency_id', '!=', 12)]}</attribute>
        </xpath>

        </field>
    </record>
</odoo>
